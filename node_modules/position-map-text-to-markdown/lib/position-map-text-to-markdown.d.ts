/// <reference types="structured-source" />
import TextSource = require("structured-source");
/**
 * get markdown location info that match `text`
 * @param {string} text
 * @param {string} markdown
 * @returns {{
 *  markdown:string,
 *  range: [number, number],
 *  loc: {
 *    start: {
 *       line:number,
 *       column:number
 *    },
 *    end: {
 *       line:number,
 *       column:number
 *    }
 * }
 * }[]}
 */
export declare function findAllPositions({text, markdown}: {
    text: string;
    markdown: string;
}): {
    markdown: string;
    range: any[];
    loc: {
        start: TextSource.SourcePosition;
        end: TextSource.SourcePosition;
    };
}[];
